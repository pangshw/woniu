package ${package}.domain.repository;

import com.woniu.domain.BaseRepository;
import ${package}.domain.entity.${entity};

/**
* ${name}
*/
public interface ${domain}Repository extends BaseRepository<${entity}> {
}
